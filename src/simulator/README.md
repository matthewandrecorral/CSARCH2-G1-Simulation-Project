# Simulator Module

This directory is reserved for the browser-independent cache engine, validation, sequence generators, replacement policies, trace types, and timing calculations.

No simulator logic is implemented during Step 2. The shared engine begins in Step 3, while LRU/MRU policy behavior and required sequence generators begin in Step 4.
